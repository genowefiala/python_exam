import pwd, grp

for p in pwd.getpwall(): 
        print p[0], grp.getgrgid(p[3])[0]



#or 


a = open('/etc/passwd', 'r')
	a.read()
	# or read lines: 
	a.readlines()
	
	# methods of the read 
	dir(a)
